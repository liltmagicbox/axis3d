# WORKFLOW — 작업 규칙

일만 벌리고 완수 못 하는 패턴을 끊기 위한 규칙. 규칙은 5개뿐이다.
(배경: 2026-09 작업 스타일 논의. 통찰이 증명되는 순간 손을 떼는 패턴 —
보상이 선불인 추상화 설계만 하고, 후불인 배선/완주 구간을 안 넘어감.)

## 1. 일의 단위는 데모다
- "시스템을 만든다"는 작업이 아니다. "___가 눈에 보인다"만 작업이다.
- 세션 시작 전에 한 줄 쓴다: `끝나면 ___가 돌아간다/보인다.`
- 이 한 줄이 안 써지면 그건 실험이다 → 규칙 3.

## 2. 추상화는 후불
- 클래스/팩토리/인터페이스를 미리 설계하지 않는다.
- 데모 안에서 같은 코드가 세 번째 중복될 때, 그때 뽑는다.
- 설계 욕구가 올라오면: 주석 스케치까지는 OK. 단 IDEAS.md에 한 줄 남기고 데모로 복귀.

## 3. 실험은 lab/에서, 판결문 필수
- 벤치마크/API 탐색 파일은 `lab/` 폴더에만 만든다.
- 파일 맨 위 두 줄을 강제한다:
  ```
  # 질문: np.concatenate가 append 루프보다 빠른가?
  # 판결: 빠름. 본채 axis.py expand()에 채택. (판결 전이면 비워두되, 비어있으면 미완)
  ```
- 판결 없이 다음 실험 금지. (websocket gen1~gen4 같은 재소송 방지책)

## 4. 새 전선은 IDEAS.md에 한 줄
- 열린 전선은 항상 하나.
- 새 일을 벌이고 싶은 충동 = 파일을 여는 대신 IDEAS.md에 한 줄 적는다.
- 충동의 8할은 "아이디어를 잃기 싫다"라서, 적으면 해소된다.

## 5. 후불 구간은 Claude에게
- 분업: 스케치/통찰/설계 판단 = 나. 배선/완주/테스트 고정/디버깅 = Claude.
- 스케치가 나오면 그대로 넘긴다: "이거 end-to-end로 돌게 만들어줘" + 완료 조건 한 줄.
- 세션 산출물은 반드시 돌아가는 상태 + 테스트로 고정된 상태로 커밋받는다.

---

## 완성의 정의 (미정 — 정해지면 여기 적는다)

완성물엔 보는 눈이 필요하다. 후불 구간을 견인하는 건 규칙이 아니라 관객이다.
- [ ] 나에게 "완성"은 무엇인가: 플레이 가능한 게임 / 브라우저 데모 링크 / 설계 증명?
- [ ] 누구에게 보여줄 것인가: (친구 한 명, 데모 링크 올릴 곳 하나라도)
